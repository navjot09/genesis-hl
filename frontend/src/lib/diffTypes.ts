/**
 * Re-export of the SHARED backend/frontend contract — do not redefine types
 * here. Source of truth: functions/src/shared/contracts.ts (via @contracts).
 */
export type { DiffRow, DiffRowKind, FileChange, SseEvent, SsePayload } from '@contracts'
